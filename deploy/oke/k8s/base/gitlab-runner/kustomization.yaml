apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: ci
resources:
  - serviceaccount.yaml
  - rbac.yaml
  - deployment.yaml
# Note: BUILD_IMAGE in the Deployment is an env value (not a container image), so
# it is not rewritten by kustomize `images`. 60-register-runner.sh sed-replaces the
# REGISTRY placeholder with the OCIR namespace before applying this overlay.
